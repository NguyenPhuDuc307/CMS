namespace CMS.Utils.Constants;

public enum Theme
{
    light,
    dark
}

public enum Language
{
    en,
    vi
}

public enum Direction
{
    LTR,
    RTL
}

public enum Color
{
    BLUE_THEME,
    AQUA_THEME,
    PURPLE_THEME,
    GREEN_THEME,
    CYAN_THEME,
    ORANGE_THEME
}

public enum Layout
{
    vertical,
    horizontal
}

public enum Sidebar
{
    mini,
    full
}
