namespace CMS.Utils.Constants;

public enum Theme
{
    Light,
    Dark
}

public enum Language
{
    English,
    German,
    Vietnamese
}

public enum Direction
{
    LTR,
    RTL
}

public enum Color
{
    BLUE_THEME,
    AQUA_THEME,
    PURPLE_THEME,
    GREEN_THEME,
    CYAN_THEME,
    ORANGE_THEME
}

public enum Layout{
    Vertical,
    Horizontal
}

public enum Container
{
    Fluid,
    Boxed
}

public enum Sidebar
{
    Default,
    Fixed
}

public enum Header
{
    Default,
    Fixed
}

public enum Card{
    Border,
    Shadow
}